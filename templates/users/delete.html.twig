{% extends 'base.html.twig' %}

{% set pageTitle = 'Delete User' %}

{% block body %}
    <div class="row">
        <div class="col-xs-12 col-md-8">
            <div class="box">
                <div class="box-header">
                    <div class="box-title">
                        <div class="box-title__title">Delete User</div>
                    </div>
                </div>
                <div class="box-body">
                    <p>Are you sure you want to delete this user. This can't be undone.</p>
                    {{ form(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
